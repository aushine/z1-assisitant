# ===== 后端 API 基础地址 =====
# 开发环境：走 vite proxy 时用 /z1/api/v1 即可（第十三轮起 /z1 服务前缀）
#           （vite.config.ts 的 server.proxy 把 /api 代理到 http://localhost:8090）
# 生产环境：填写真实 API 地址，如 https://api.life.app/z1/api/v1
VITE_API_BASE=/z1/api/v1

# ===== 应用标题 =====
VITE_APP_TITLE=生活助手

# ===== 是否启用 Mock 数据 =====
# '1' 启用（未来接 mock server），'0' 关闭
VITE_USE_MOCK=0

# （可选）上传静态 /z1/uploads/ 单独代理目标；不填 = 同 VITE_API_PROXY_TARGET。
# 一般不用填：图片地址改由后端 storage.public_url 下发（见 utils/avatar.ts）
