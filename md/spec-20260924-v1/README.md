# Spec · 2026-09-24 · v1

> **批次名**：移动端体验优化 + 习惯/待办二级分类
> **基准**：`md/spec/`（2026-09-24 代码实测）
> **本轮定位**：这是一批**改动设计**（要落地的新东西），落地验收后结论回写 `md/spec/`，再开 v2。
> **冲突优先级**：代码 > `md/spec/` > 本批 > `md/archive/`。

---

## 0. 本批范围一览

| # | 项 | 性质 | 端 | 涉及模块 |
|---|---|---|---|---|
| **R1** | 登录页：去掉重复 Z1 图标；修 slogan 被裁 | 🔧 bug | 移动 | 登录 |
| **R2** | 待办/习惯「完成列表」图标偏小 → 统一 IconBox | 🎨 一致性 | 移动 | 任务 / 习惯 |
| **R3** | 待办 + 习惯**二级分类**（内置种子 + 用户增删） | 📋 需求 | **双端** | 任务 / 习惯 / 数据模型 |
| **R4** | 分类管理：去掉页内分段，各自入口独立 | 📋 需求 | **双端** | 我的 |
| **R5** | 周期（周/月/年）切换改为**下拉/弹层** | 📋 需求 | **双端** | 财务 / 流水 |
| **R6** | 流水卡片 `replay` 图标 → 换成 toggle 图标 | 🔧 小改 | 移动 | 财务 |
| **R7** | 记一笔：计算盘与金额框**组成一体计算器** | 📋 需求 | **双端**（桌面形态不同） | 财务 · 记一笔 |

> **拆分依据**（用户 2026-09-24 明确）：R1/R2/R6 属「能顺手改的小 bug / 一致性」，可**直接改**，
> 但本批统一走 spec 归档，避免散落。R3/R4/R5/R7 是**需求**，必须先设计。

---

## 1. 文档结构

```
md/spec-20260924-v1/
├── README.md            ← 本文（范围、约定、验收）
├── 01-登录页修复.md      R1
├── 02-完成列表图标统一.md R2 / R6
├── 03-习惯与待办二级分类.md R3（含数据模型 + 种子 + 接口 + 迁移）
├── 04-分类管理改造.md     R4
├── 05-周期切换交互.md     R5
└── 06-记一笔计算器.md     R7
```

---

## 2. 全局约定（沿用 md/spec 铁律）

1. **默认双端覆盖**：R3/R4/R5/R7 必须同时给移动端 + 桌面端方案；R1/R2/R6 仅移动端（桌面端无此问题）。
2. **不确定先问**：本批设计阶段已与用户确认（见 §3 决策留档）。
3. **改了要回写**：本批验收后，把结论回写 `md/spec/` 对应章节（至少 03 数据模型、06 任务、07 习惯、08 财务、01 信息架构）。

---

## 3. 决策留档（用户 2026-09-24 确认）

| # | 问题 | 用户决策 |
|---|---|---|
| D1 | 二级分类是否做 | **做**（推翻原 D21「只做一级」） |
| D2 | 二级分类来源 | **内置种子**（参考财务分类做法，`utility` 里写种子） |
| D3 | 用户可否增删二级 | **可以**，参考收支流水分类的增删交互 |
| D4 | 统计数据归集 | **要**：记在二级上 → 归到一级统计（参考财务 `SumExpenseByCategory` 子分类归集） |
| D5 | 现有数据迁移 | **要迁移**；迁移脚本由 agent 执行（数据库在 Pi） |
| D6 | 周期切换交互形式 | **B：下拉/弹层**（不做 A 分段控件） |
| D7 | 「旋转图标」具体位置 | `SummaryCards.vue` 卡片里的 `replay` 图标 |
| D8 | 桌面端 | 由 agent 设计（双端覆盖） |

---

## 4. 验收标准（逐条）

- [ ] R1 登录页只有**一个** Z1 标识；slogan 完整可见、不被裁切、不被表单卡压住
- [ ] R2 待办/习惯完成列表图标尺寸与收支列表一致（IconBox 40 / 图标 20 档）
- [ ] R3 二级分类：内置种子落地、可增删、记在二级上统计归到一级、存量数据迁移完成且幂等
- [ ] R4 分类管理：待办入口进去只显示待办分类（标题「待办分类管理」），习惯同理，无页内分段切换
- [ ] R5 周期切换：下拉/弹层选择周/月/年，双端一致，选择持久化
- [ ] R6 卡片图标不再像「刷新」，换成明确的 toggle/切换图标
- [ ] R7 计算盘与金额框视觉上是一体的计算器（零缝隙、共用圆角、同底色），无「图标跑到上面」
- [ ] 全部改动 push 后，Pi 构建部署通过（`pi-build-deploy.sh all` 自检全绿）
- [ ] 结论回写 `md/spec/`
